const fs = require('fs'); let c = fs.readFileSync('f:/AntiPiry/server.js.old', 'utf8'); c = c.replace('const path = require(\" "path\);', 'const path = require(\path\);\nconst { db, STORAGE_TYPE } = require(\./db/index.js\);'); c = c.replace('console.log(\Сервер" "запускается...\);', 'console.log(\[DB]" Тип "хранилища:\, STORAGE_TYPE); console.log(\Сервер" "запускается...\);'); fs.writeFileSync('f:/AntiPiry/server.js', c); console.log('done')  
